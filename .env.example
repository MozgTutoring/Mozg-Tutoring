# SMTP for your domain (must be authorized to send as FROM_EMAIL)
SMTP_HOST=smtp.yourdomain.tld
SMTP_PORT=587
SMTP_USER=postmaster@yourdomain.tld
SMTP_PASS=CHANGE_ME

# Must be an address at your domain to satisfy DMARC alignment
FROM_EMAIL="Website Contact <contact@yourdomain.tld>"
TO_EMAIL=owner@yourdomain.tld

# DKIM settings (align with your domain and selector)
DKIM_DOMAIN=yourdomain.tld
DKIM_SELECTOR=mail
# Paste your PEM private key content as a single line or use a file reader in code
DKIM_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"